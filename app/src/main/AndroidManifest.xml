<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="学习打卡"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test5"
        tools:targetApi="31">
        <activity
            android:name=".subActivity.SubLoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".subActivity.SubCreateTargetActivity"/>
        <activity android:name=".subActivity.SubDecorationActivity"/>
        <activity android:name=".subActivity.SubSettingActivity"/>
        <activity android:name=".subActivity.SubStatisticActivity"/>
        <activity android:name=".subActivity.SubAttendance"/>
        <activity android:name=".MainActivity"/>
    </application>

</manifest>